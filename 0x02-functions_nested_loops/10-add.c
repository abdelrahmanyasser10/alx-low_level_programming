#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 *
 * @m: first integer
 * @a: second integer
 *
 * Return: the sum of the m + a
 */
int add(int m, int a)
{
	return (m + a);
}
