#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - entry point
 *
 * @argc: length of arguments
 * @argv: string of arguments
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc + strlen(char **argv) - strlen(char **argv));
	return (0);
}
